# OpenSpec Configuration — Alis
# Auto-generated by sdd-init on 2026-06-05

context: |
  Alis is a fully managed high-performance multimedia, application, tooling, and
  game development ecosystem written in C#. .NET monorepo with layered architecture:
  1_Presentation → 2_Application → 3_Structuration → 4_Operation → 5_Declaration → 6_Ideation.
  Targets net8.0+ / netstandard2.0, C# 13, LangVersion 13. SDK 10.x with rollForward latestMajor.
  Cross-platform (macOS, Windows, Linux, Web, Mobile).

strict_tdd: true
strict_tdd_source: copilot-instructions.md (line 787)

testing:
  runner:
    command: dotnet test alis_design.sln -c Release -f net8.0
    framework: xUnit 2.6.6
  layers:
    unit:
      available: true
      tool: xUnit + Moq 4.20.70 + Xunit.StaFact 1.1.11
    integration:
      available: false
      tool: —
    e2e:
      available: false
      tool: —
  coverage:
    available: true
    command: coverlet.collector 6.0.4 (via dotnet test)
  quality:
    linter: dotnet build (analyzers on demand)
    formatter: .editorconfig + ReSharper config
    type_checker: dotnet build (MSBuild)

phases:
  proposal: openspec/specs/
  spec: openspec/specs/
  design: openspec/specs/
  tasks: openspec/specs/
  apply: src/
  verify: dotnet test
  archive: openspec/changes/archive/
