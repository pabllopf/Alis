<?xml version="1.0" encoding="utf-8"?>
<Project>
    <PropertyGroup Condition="$(AssemblyName.Contains('Generator')) == False AND '$(Configuration)'=='Release'">
        <Optimize>true</Optimize>
    </PropertyGroup>
</Project>