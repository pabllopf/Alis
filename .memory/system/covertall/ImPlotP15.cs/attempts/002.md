# Attempt 002

Target:
ImPlotP15.cs

Initial line coverage (from attempt 001):
95.09% (213/224)

Initial branch coverage:
100%

Gaps:
- 11 closing-brace sequence points of the uint/long/ulong PlotBarGroups
  overloads (previously lines 56, 76, 97, 118, 139, 160, 181, 199, 219, 239,
  260).

Root cause:
- The PlotBarGroups wrappers marshalled the label ids as a byte[][] jagged
  array. The .NET P/Invoke marshaller rejects nested arrays with
  MarshalDirectiveException ("There is no marshaling support for nested
  arrays"). The call executed (covered) but the method's final sequence point
  (closing brace) was never reached because the exception aborted the method.

Resolution — production defect fix (rule 27: actual defect, required for
correct testability, preserves intended behavior, AOT-compatible, documented):
- ImPlotNative.cs: ImPlot_PlotBarGroups_U32Ptr / _S64Ptr / _U64Ptr DllImport
  declarations changed from byte[][] labelIds to
  [MarshalAs(UnmanagedType.LPArray, ArraySubType = UnmanagedType.LPStr)]
  string[] labelIds (marshal as const char**).
- ImPlotP15.cs: 11 PlotBarGroups wrappers simplified to pass labelIds
  directly (removed the byte[][] building loop).
- ImPlotP2.cs: uint count-only PlotBarGroups wrapper updated for compilation.
- ImPlotP15ExecutionTests.cs: removed the dead MarshalDirectiveException
  try/catch blocks; the overloads now execute against the native library.
- De-risked with a standalone probe: native ImPlot_PlotBarGroups_U32Ptr call
  with string[] marshalling completes inside a real plot without exception or
  native assert.

Test result:
PASS (full project: 7660 passed, 14 skipped, 0 failed)

Coverage result:
100% lines, 100% branches for ImPlotP15.cs

Commit:
b613fda2d

Remaining:
none