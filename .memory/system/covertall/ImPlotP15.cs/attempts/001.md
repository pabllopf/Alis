# Attempt 001

Target:
ImPlotP15.cs

Initial line coverage:
95.09% (213/224)

Initial branch coverage:
100%

Gaps:
- 11 closing-brace sequence points of the uint/long/ulong PlotBarGroups
  overloads (lines 56, 76, 97, 118, 139, 160, 181, 199, 219, 239, 260).

Root cause:
- The PlotBarGroups wrappers marshal the label ids as a `byte[][]` jagged
  array. The .NET P/Invoke marshaller rejects nested arrays with
  MarshalDirectiveException ("There is no marshaling support for nested
  arrays"). The native call line executes (covered) but the method's final
  sequence point (closing brace) is never reached because the exception
  aborts the method. Verified empirically: empty and non-empty label arrays
  both throw; the rejection is on the TYPE, so no input can complete the
  method.

Tests created:
- none possible. Calling the wrapper from a test always throws
  MarshalDirectiveException at the call; no input, parameter combination, or
  context can let the method complete.

Production fix:
- Not performed. Would require redesigning the label-id marshalling
  (unsafe/IntPtr). Repository convention treats this as out of scope
  (documented in Ui state.md; same pattern accepted in ImPlotP2.cs).

Test result:
PASS (full project: 7660 passed, 14 skipped, 0 failed)

Coverage result:
95.09% lines, 100% branches — maximum achievable

Commit:
none (no code changes)

Remaining:
- The 11 closing-brace sequence points (structurally uncoverable).

Resolved by attempt 002 (production marshalling fix); state reclassified to COMPLETED.
