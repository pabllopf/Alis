# Attempt 001

Target: 6_Ideation/Memory/src/AssetRegistry.cs
Project: Alis.Core.Aspect.Memory

Initial line coverage: 97.4% (260/267)
Initial branch coverage: 98/102 branch paths (96.1%)

Production code analyzed: full AssetRegistry.cs (660 lines). Coverage report parsed from ./.test/<guid>/coverage.opencover.xml (OpenCover, net8.0, Debug).

Gaps identified at baseline (all previously exercised by 96 passing tests across 9 AssetRegistry test files):

1. Lines 392,393,395 + none extra — `catch` block around `File.SetLastWriteTimeUtc(tempFilePath, ...)` in ExtractResourceToTemp.
2. Lines 500,501 — `return string.Empty;` for `bytes.Length == 0` in `ToLowerHex(ReadOnlySpan<byte>)` (the NETSTANDARD2_0 overload is not compiled on net8.0).
3. Lines 541,542 + branch 540 p0 — `InvalidOperationException` when the active assembly has no registered loader in `EnsureZipCachedForActiveAssembly`.
4. Branch line 377 p0 — `Path.GetDirectoryName(tempFilePath) ?? Path.GetTempPath()` null-coalesce.
5. Branch line 434 p0 — `Path.GetExtension(normalizedResourceKey) ?? string.Empty` null-coalesce.
6. Branch line 499 p0 — `bytes.Length == 0` true path of `ToLowerHex`.

Reachability analysis performed (public API only, no reflection, no dynamic code):

- (1) The temp file is created and closed by the same statement sequence immediately before `SetLastWriteTimeUtc` is called. On macOS/APFS the call succeeds for every representable DateTime; ambient probe confirmed OK for dates 1601-01-02, 1677-09-21, 1900, 1969-12-31, 9999-12-31, DateTime.MaxValue and DateTime.MinValue. The two preceding guards (file must exist and match) make a deterministic local failure impossible; the pre-existing race-based test `ExtractResourceToTemp_SetLastWriteTimeUtcFails_CatchBlockHandles` (AssetRegistryCoverageTest.cs:549) attempts deletion races and still does not reach the catch block. Deleting the file between create and set is not orchestrable deterministically without a filesystem hook (reflection/runtime emit are forbidden).
- (2) `ToLowerHex` is only called with the 32-byte SHA-256 digest of a normalized resource key; the key is guaranteed non-empty by the whitespace guard, so the empty-span path is unreachable.
- (3) `ActiveAssemblyName` is assigned only by `RegisterAssembly`, which always stores a loader under that exact key; loaders are never removed. Both public callers (`GetResourceMemoryStreamByName`, `GetResourcePathByName`) validate `RegisteredAssetLoaders.ContainsKey(ActiveAssemblyName)` before reaching `EnsureZipCachedForActiveAssembly`, so the miss path cannot be reached even across concurrent calls.
- (4) `tempFilePath` is always `Path.GetTempPath()+MakeSafeTempName(...)`, i.e. an absolute path with a parent directory, so `GetDirectoryName` never returns null.
- (5) `Path.GetExtension` returns null only for a null argument; `normalizedResourceKey` is never null.
- (6) see (2).

Tests created: none new. All reachable behavior of the file is already covered by the 96 existing tests (xUnit, [Fact] only). Adding tests that call unreachable code would require reflection or artificial production branches, both forbidden by the coverage rules (rules 7, 9, 26, 27).

Test result: 96/96 pass (baseline run).
Coverage result: line 97.4%, branch 96.1% — unchanged, because every remaining uncovered sequence point/branch path is compile-time unreachable or deterministically unreachable in this test environment.

Remaining: as listed in state.md — all documented as unreachable per rule 26 (BLOCKED justification above).
