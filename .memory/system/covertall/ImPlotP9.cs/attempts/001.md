# Attempt 001

Target:
ImPlotP9.cs

Initial line coverage:
90.5% (201/222)

Initial branch coverage:
100% (42/42)

Gaps:
- 21 PlotPieChart overloads each with 1 uncovered sequence point (7/8).

Analysis:
- The uncovered point in each PlotPieChart overload is the method-closing
  brace, which requires the native call statement to COMPLETE. The native
  P/Invoke (ImPlotNative.ImPlot_PlotPieChart_*) declares a byte[][] labelIds
  parameter, and the default .NET interop marshaler has no support for nested
  arrays: every invocation throws MarshalDirectiveException at the call site.
- New tests call every overload with valid non-null labels; they assert the
  MarshalDirectiveException and thereby cover the call-site sequence point
  (verified vc=2 at L298 for the float overload). The closing brace (L299)
  remains unreachable because the call can never return successfully.
- An earlier context-based variant (CreateContexts/BeginPlot per the existing
  ImPlotP9ExecutionTests pattern) crashed the test host with a native ImGui
  assertion because the MarshalDirectiveException left BeginPlot un-ended;
  the finally-guarded version confirmed the managed exception type.
- Making the call succeed requires changing the production P/Invoke signature
  (e.g. IntPtr-based marshaling) - a production change, forbidden for coverage
  purposes.

Tests created:
- PlotPieChart_Float_Overloads_Execute
- PlotPieChart_Double_Overloads_Execute
- PlotPieChart_SByte_Overloads_Execute
- PlotPieChart_Byte_Overloads_Execute
- PlotPieChart_Short_Overloads_Execute
- PlotPieChart_UShort_Overload_Executes

Test result:
PASS (full Ui suite passes)

Final line coverage:
90.5% (201/222)

Final branch coverage:
100% (42/42)

Commit:
test: cover PlotPieChart native call sites of ImPlotP9.cs

Remaining:
- 21 method-completion braces, unreachable because the byte[][] parameter of
  the PlotPieChart P/Invokes is unmarshalable (MarshalDirectiveException at
  every call). Reaching them requires a production marshaling fix.

Status:
BLOCKED (branch coverage 100%; the remaining 21 lines cannot execute while
the P/Invoke signature declares an unmarshalable byte[][]).