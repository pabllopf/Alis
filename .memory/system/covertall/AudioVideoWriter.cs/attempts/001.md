# Attempt 001

Target: AudioVideoWriter.cs
Agent: covertall-avw-001
Timestamp: 2026-08-16T20:20:00Z

Initial line coverage: 99.4% (176/177)
Initial branch coverage: 100% (78/78)

Uncovered lines:
- 379 (catch)
- 380 ({)
- 382 (})

Uncovered branches: none

Production code analyzed:
- CloseWrite() inner try/catch around HasExited/Kill (lines 370-385)
- Determined the catch only triggers when Ffmpegp.WaitForExit(5000) returns false
  (process still running) AND HasExited or Kill throws.
- Experimentally verified (standalone net8.0 harness) that disposing the Process
  handle via Process.Close() DURING the in-flight WaitForExit(5000) keeps the
  wait polling (returns false), while the subsequent HasExited call throws
  InvalidOperationException("No process is associated with this object").
- The final WaitForExit() then propagates the same InvalidOperationException.

Tests created:
- CloseWrite_WhenProcessHandleDisposedDuringWait_ThrowsAfterSwallowingKillError:
  opens a writer with a fake ffmpeg script via OpenWrite(), runs CloseWrite on a
  background task, disposes the FFmpeg process handle mid-wait, asserts the
  swallowed error path (catch), the propagated InvalidOperationException, and
  that OpenedForWriting is reset in the finally block. Cleans up the orphan
  process with /bin/kill.

Test result: PASS (full project: 1557/1557 passed)

Final line coverage: 100% (179/179)
Final branch coverage: 100% (78/78)

Commit: pending

Remaining gaps: none

Approach notes:
- No reflection, no dynamic code, no Moq. Only public API (OpenWrite,
  CurrentFFmpegProcess, Process.Close) plus real process orchestration.

Final commit: 6905abd98
