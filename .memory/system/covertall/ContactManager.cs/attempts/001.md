# Attempt 001

Target:
ContactManager.cs

Initial line coverage:
76.3% (261/342)

Initial branch coverage:
77.9% (109/140)

Gaps:
- CollideMultiCore, ProcessContactMultiCore, UpdateContactWithLock,
  AcquireLocks fully uncovered (multi-core pipeline gated behind
  CollideMultithreadThreshold = int.MaxValue, so unreachable via Collide()).
- ProcessContactCollision disabled-body branch (L542) uncovered.
- AddPair null-contact path (L179-181) uncovered.

Analysis:
- InternalsVisibleTo allows direct invocation of internal methods, which is the
  established repo pattern (existing tests call internal Contact.Create /
  ContactManager.Destroy directly).
- Multi-core pipeline reached by calling CollideMultiCore() and
  UpdateContactWithLock() directly with standalone bodies (Body() public ctor,
  fixtures registered into a DynamicTreeBroadPhase via Fixture.CreateProxies,
  contacts created via ContactManager.AddPair). Bodies have no world, so
  disabling a body does not destroy its contacts and the disabled branch is
  reachable.
- Concurrent agent later added a 10s lock-acquisition timeout + spin/sleep
  backoff to AcquireLocks; covered via a test that holds one lock permanently
  and asserts the timeout exception.
- FilterFlag path covered by flagging a contact and setting ContactFilter to
  reject it.

Tests created:
- CollideMultiCore_WithOverlappingContacts_UpdatesAllContacts
- CollideMultiCore_WithSeparatedContacts_DestroysThem
- CollideMultiCore_WithDisabledBody_KeepsContact
- CollideMultiCore_WithDisabledBodyA_KeepsContact
- CollideMultiCore_WithFilterFlaggedContact_DestroysIt
- CollideMultiCore_WithSleepingBodies_KeepsContact
- Collide_WithDisabledBodyA_ReturnsNextContact
- Collide_WithDisabledBodyB_ReturnsNextContact
- UpdateContactWithLock_WhenLockOrdersAreEqual_ThrowsInvalidOperationException
- UpdateContactWithLock_WhenBodyALocked_RetriesAndCompletes
- UpdateContactWithLock_WhenBodyBLocked_RetriesAndCompletes
- UpdateContactWithLock_WhenBodyALockOrderGreaterThanBodyB_SwapsLockOrder
- UpdateContactWithLock_WhenLockHeldBeyondTimeout_ThrowsInvalidOperationException

Test result:
PASS (4115/4115)

Final line coverage:
98.6% (348/353)

Final branch coverage:
97.9% (141/144)

Commit:
pending

Remaining:
- AddPair L181-182: Contact.Create returns null only when private static
  ReturnNullOverride (Contact.cs:48) is true; never set anywhere in the
  codebase. Statically unreachable without reflection or production change.
- Collide L329-331 and L328 branches: the multi-core gate
  (ContactCount > CollideMultithreadThreshold) can never be true because
  CollideMultithreadThreshold is a public readonly field fixed to int.MaxValue
  (line 56) with no constructor assignment and no other writer. Statically
  impossible.

Status:
BLOCKED (remaining 5 sequence points + 3 branch points are statically
unreachable; reaching them would require reflection on private state
(ReturnNullOverride) or changing production code, both forbidden).