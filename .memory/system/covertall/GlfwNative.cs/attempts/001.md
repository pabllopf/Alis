# Attempt 001 — GlfwNative.cs

Timestamp: 2026-08-17
Agent: cover-agent-001
Target: 1_Presentation/Extension/Graphic/Glfw/src/GlfwNative.cs

## Initial coverage

- Line: 91.43% (128/140) — best reliable instrumented run
- Branch: 72.22% (13/18)

## Gaps

- 384-387 + 383/path1: GetJoystickHats loop body (needs joystick with hats)
- 1714-1716 + 1713/path0: GetJoystickAxes count>0 body (needs joystick with axes)
- 1731-1733 + 1730/path1: GetJoystickButtons loop body (needs joystick with buttons)
- 412/path0, 478/path0: GetJoystickGuid/GetGamepadName null path
  (needs the guarded tests to run with GlfwTestBootstrap.Ready == true in an
  instrumented process)
- 2002-2003: GlfwError private callback throw (needs a GLFW error to fire)

## Analysis

- Hardware: no joystick/gamepad is connected (JoystickPresent(0..15) == false,
  hats/buttons/axes all return 0). The present-device loop bodies and count>0
  branches are unreachable without physical input hardware. BLOCKED.
- Platform: GlfwNative's static ctor calls glfwInit() (lines 62-65). On macOS,
  GLFW must be initialized on the process main thread. Any test calling a
  GlfwNative method on an xUnit worker thread without the main-thread hook
  HANGS the process (verified: 5 candidate [RequireGlfwFact] tests calling
  GetJoystickGuid/GetGamepadName/GetJoystickAxes/GetJoystickButtons/GetJoystickHats
  hung the suite; removing them restored the 596-test run to 112ms). The repo's
  own tests therefore gate every GlfwNative call behind
  `[RequireGlfwFact]` + `if (!GlfwTestBootstrap.Ready) return;`.
- Hook flakiness: with the repo startup hook (ALIS_GLFW_HOOK=1 +
  DOTNET_STARTUP_HOOKS, plus a temporary preload hook outside the repo to
  satisfy the Memory module initializer), coverage runs are inconsistent:
  when GlfwNative is instrumented (91.43%), Ready is false so guarded tests
  early-return; when Ready is true, the preload races coverlet instrumentation
  (reports 0%). GameWindow.cs reached 100% via the same hook, confirming the
  hook runs and creates windows on the main thread.
- The GlfwError error path (2002-2003) was probed via
  GlfwNative.GetClientApi(Window.None) but any such call hangs the process on
  macOS for the same main-thread reason.

## Tests created

A candidate GlfwNativeCoverageTests.cs (5 joystick tests) was written, measured
to hang the suite, and removed. No committed tests.

## Test result

PASS — 596/596 (exclusion-filtered run, without the concurrent agent's
in-flight GameWindowConstructorTests).

## Coverage result

- Line: 91.43% (best reliable)
- Branch: 72.22%

## Improvement

Not achievable without hardware and a reliably-instrumented main-thread hook.

## Commit

None.

## Remaining

Joystick present-device paths (hardware), missing-device paths gated behind the
flaky hook, and the error-callback path (hangs on macOS). Status: BLOCKED.