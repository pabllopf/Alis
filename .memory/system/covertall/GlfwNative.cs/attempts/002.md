# Attempt 002 — GlfwNative.cs (definitive verification)

Timestamp: 2026-08-18
Agent: cover-agent-001
Target: 1_Presentation/Extension/Graphic/Glfw/src/GlfwNative.cs

## State at start

- Line: 91.43% (128/140), Branch: 72.22% (13/18) — 09:09 hook run.

## What was verified this iteration

1. **Ready=true is achievable and reliable** when running the suite with the
   repo startup hook (ALIS_GLFW_HOOK=1 + DOTNET_STARTUP_HOOKS + temporary
   preload hook) and `--no-build`: a diagnostic test reported
   `Ready=True, Failures=0` both without and with the coverlet profiler
   (`--collect`). The hook creates windows and the worker completes all steps.

2. **The joystick gated tests really run** in that configuration
   (GetJoystickGuid_MissingDevice_ReturnsNull, GetGamepadName_MissingDevice_ReturnsNull,
   GetJoystickAxes_MissingDevice_ReturnsEmpty, GetJoystickButtons_MissingDevice_ReturnsEmpty,
   GetJoystickHats_MissingDevice_ReturnsNone — all executed native calls, µs
   durations). The coverage result remains 91.43% / 72.22%.

3. **Corrected branch semantics**: OpenCover path 1 = condition TRUE, path 0 =
   FALSE. The uncovered branches therefore are:
   - 383/p1 (GetJoystickHats loop entered): needs a joystick WITH hats.
   - 412/p0 (GetJoystickGuid returned non-null): needs a connected joystick.
   - 478/p0 (GetGamepadName returned non-null): needs a connected gamepad with mapping.
   - 1713/p0 (GetJoystickAxes count > 0): needs a joystick WITH axes.
   - 1730/p1 (GetJoystickButtons loop entered): needs a joystick WITH buttons.
   All hardware-dependent. No joystick is connected (JoystickPresent false for
   all ids; hats/buttons/axes all 0).

4. **GlfwError path (2002-2003)**: a scratch console probe (running on the true
   main thread) confirmed glfwInit succeeds, and an invalid window hint
   (`WindowHint((Hint)99999, 1)`) fires the error callback → GlfwError throws
   InvalidOperationException ("Invalid window hint 0x0001869F"). However the
   exception ESCAPES all managed catch handlers (bypasses try/catch, terminates
   the process as unhandled) because the homebrew libglfw build lacks unwind
   tables, so the runtime cannot unwind the native frames. An equivalent test
   would crash the test host. The earlier candidate using
   GetClientApi(Window.None) was also verified to HANG the process. Both trigger
   paths are therefore unusable in-process.

## Tests created

- GlfwNativeErrorPathTests.cs (2 hook-gated tests) — removed after the probe
  proved the exception escapes managed handlers (would crash the test host).
- GlfwReadyDiagTests.cs (diagnostic) — removed.

## Test result

PASS — 598/598 (plus 3 skipped by the concurrent agent's RequireNoGlfwFact) in
the definitive hook run.

## Coverage result

- Line: 91.43% (128/140)
- Branch: 72.22% (13/18)

## Improvement

None possible. All remaining uncovered lines/branches require either physical
input hardware or a GLFW error path that crashes/hangs the process on macOS.

## Commit

None.

## Remaining

Hardware loops (384-387, 1714-1716, 1731-1733) and non-null joystick paths
(412/p0, 478/p0) — require a connected joystick/gamepad. GlfwError (2002-2003)
— exception escapes managed handlers (no unwind tables in homebrew GLFW).
Status: BLOCKED.