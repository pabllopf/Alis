# Attempt 002 — SonarCloud-driven gap check for the SFML dashboard files

Timestamp: 2026-09-12
Agent: covertall-sfmltime

User instruction: implement unit tests focused on the parts SonarCloud reports as uncovered.

SonarCloud project: pabllopf-official_alis-extension-graphic-sfml (last analysis 2026-08-24; dashboard lists many files at 0.0%).

Fresh measurement (mandatory coverage command, opencover report):

| File | SonarCloud | Fresh local | Action |
|---|---|---|---|
| Systems/SfmlTime.cs | 0/26 | 26/26, 2/2 branches | already complete |
| Windows/SizeEventArgs.cs | 0/10 | 10/10 | already complete |
| Windows/Sensor.cs | 0/5 | 5/5 | already complete |
| Render/SfmlText.cs | 0/84 | 80/84, 7/10 branches | BLOCKED (see below) |
| Render/Shape.cs | 0/60 | 56/60, 3/6 branches | BLOCKED (see below) |
| Audios/Sound.cs | 0/53 | 0/53 | BLOCKED (see below) |

Why not 100% on the dashboard: the Sonar scan CI job (macos-15-intel) does not install CSFML,
so all RequireCSfml* gated tests skip and coverage uploads 0% for native-dependent files.
Fixing that requires building/brew-installing CSFML in `.github/workflows/...` (shared resource)
→ NEEDS_GLOBAL_CHANGE, not fixable from a test-only agent.

## Empirical probes performed (evidence for SOUND blocker)

Standalone probe (/tmp/opencode/sfmlprobe) against /opt/homebrew/lib/libcsfml-*.dylib:
- sfSeconds(1) works → returns 1_000_000 µs (csfml-system fine).
- sfSoundBuffer_createFromFile("/nonexistent.wav") returns 0 without crash.
- sfSoundBuffer_createFromFile(valid .wav on disk) → process dies before returning.
- sfSoundBuffer_createFromSamples (managed short[] AND pinnedIntPtr variants) → process dies.
→ ALL csfml-audio buffer/sound creation crashes the host under CSFML 3.0 on this macOS.
  Sound.cs (0/53) and SoundBuffer.cs native paths are therefore unreachable in this environment.

A trial test file (SoundSoundBufferManagedSurfaceCoverageTests.cs) crashed the test host;
it was reverted (that filename is tracked at HEAD, commit 61fc39267; restored unchanged).

## SfmlText.cs (l.263-267) / Shape.cs (l.166-170)

Draw switch-case bodies need real RenderWindow/RenderTexture native draw calls;
prior agent documented the draw calls crash the test host with the CSFML 3.0 sfRenderStates ABI
(stencil fields gained in 3.0 vs 2.x marshaling) and RenderTexture creation hangs offscreen.

Status: BLOCKED_ITEMS_ONLY
Commit: (trace only)
