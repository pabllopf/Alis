# Attempt 003

Target:
4_Operation/Physic/src/Common/TextureTools/MarchingSquares.cs (SonarCloud coverage 81.7%)

Initial line coverage:
82.90% local (412/497)

Initial branch coverage:
78.33% local (141/180)

Gaps:
- Entire CombineScanLines merge pipeline (lines 299-327) and helpers
  CanCombine / FindStartingPoint / HasValidStart / HasMatchingVertex /
  MergePolygons / UpdatePolygonReferences
- Lerp/Xlerp/Ylerp interpolation branches

Production code analyzed:
- MarchingSquares.cs regression: initial commit efe847602 had
  MarchSquare(..., x, y, ...) and ps[x, y] = ...; refactor 55cdf5d06/4021447ae
  (Sonar S3776 parameter-reduction) hard-coded row index 0 into
  MarchSquare(ctx.F, ctx.Fs, ref gp, x, 0, ...) and ctx.Ps[x, 0] = ...
  This broke vertical cell sampling and made CombineScanLines permanently
  unable to merge anything (ps[x, y] null for all y >= 1). Fixed by
  passing the row index through ProcessCell and restoring ps[x, ay].
- After the fix, all 4126 existing tests still pass.

Tests created:
- DetectSquares_WithSolidBlockAndCombine_MergesIntoSinglePolygon
  (2x2 solid block: combine=true yields exactly 1 polygon with 10 vertices;
  combine=false yields 9 polygons)
- DetectSquares_WithIsolatedCellDiagonalAndCombine_KeepsPolygonsSeparated
  (18 separated polygons merge into 2, 10 vertices each)
- DetectSquares_WithInterpolatedEdgeVertex_ProducesLerpedPoint
  (asserts interpolated vertex (1.75, 1.0))
- DetectSquares_WithSharedDegenerateEdge_UsesMidInterpolation
- DetectSquares_WithWideSolidBlockAndCombine_MergesIntoSinglePolygon
  (4x2 block: 1 polygon, 16 vertices, both rows preserved)
- DetectSquares_WithScatteredSamplesAndCombine_MergesAdjacentScanLines
  (deterministic dense field derived from randomized search seed 10:
  forces CombineScanLines skip path CanCombine u.GeomP == p.GeomP,
  UpdatePolygonReferences match/no-match paths in both loops and
  FindStartingPoint walk iterations)

Test result:
PASS (4132/4132)

Final line coverage:
99.40% (494/497)

Final branch coverage:
98.33% (177/180)

Commit:
pending

Remaining (5 points) and why they cannot be covered:

1. Lines 308-310 + branch (307,'0') — HasValidStart(...) == false:
   HasValidStart only fails when FindStartingPoint lands on an ay-line node
   whose successor leaves the ay line (ay = y*cellHeight, the cell's bottom
   edge). Points are inserted in ascending index order (i = 0..7), so all
   ay-line points form a contiguous run at the head of the list. Skipping
   leading nodes via X < ax requires a negative interpolation parameter,
   which requires v0 > 0 and v1 > v0 for i=1 — i.e. both bottom samples
   positive — which contradicts the CanCombine precondition
   (p.Key & 12) != 0 (bottom samples negative). Merged-poly lists only
   receive insertions, never reorders that break the run. Verified
   empirically: 13,400 randomized fields plus 8 targeted probe patterns
   (temporary /tmp hook instrumentation, since reverted) never executed the
   false branch; it would require an invalid state, not more tests.

2. Branch (345,'1') — `(u.Key & 3) == 0` true inside CanCombine:
   u = ps[x, y-1]; u.Key bits 1 and 2 are fs[x, y] and fs[x+1, y] — the
   same samples as p's bits 8 and 4 (bit 8/4 of the cell above). Therefore
   whenever the first guard passes ((p.Key & 12) != 0), u.Key & 3 cannot be
   0. u == null is likewise impossible because a cell whose bottom samples
   are negative always produces a non-empty polygon (LookMarch val != 0)
   and thus always a ps entry. Verified empirically over the same 13,400
   randomized runs (HOOKB1 / HOOKB2 instrumentation): zero occurrences.

