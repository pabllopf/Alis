# Attempt 001

Target:
MarchingSquares.cs

Initial line coverage:
82.3% (409/497)

Initial branch coverage:
77.8% (140/180)

Gaps:
- RemoveParallelVerticesAfterInsertion L733-736 (iterator wrap when the
  insertion node is the last vertex).
- CombineScanLines merge logic, CanCombine pass conditions, FindStartingPoint,
  HasValidStart, HasMatchingVertex, MergePolygons, UpdatePolygonReferences
  (entire polygon-merging pipeline).

Analysis:
- RemoveParallelVerticesAfterInsertion is reachable through CombLeft (internal),
  which ProcessCell calls on the (key & 9) combine path. The wrap branch
  (ai == ap.End() after NextPos) fires when the matched vertex is the last
  node of the polygon. CxFastList.Add prepends, so the tail is the first-added
  element and the head of polyb is the last-added. A match at polya's tail was
  constructed: polya = [(5,5),(2,2),(1,1)] in list order, polyb = [(5,5),(10,10),(9,9)]
  in list order, so polyb.Begin() == (5,5) matches polya's last node; the
  inserted middle element becomes the new tail, and NextPos() == End triggers
  the wrap.
- The remaining pipeline is statically unreachable: ProcessCell (line 276)
  always writes ctx.Ps[x, 0] with a hardcoded 0 row index, so every Ps[x,y]
  with y >= 1 is null. CombineScanLines therefore always fails its first
  CanCombine check (p == null at line 338) and the merge path never executes.
  All of CombineScanLines/CanCombine/FindStartingPoint/HasValidStart/
  HasMatchingVertex/MergePolygons/UpdatePolygonReferences are private with no
  callers outside this dead path (verified by repository-wide grep).

Tests created:
- CombLeft_WithMatchAtLastVertex_WrapsIterator

Test result:
PASS (4116/4116)

Final line coverage:
82.9% (412/497)

Final branch coverage:
78.3% (141/180)

Commit:
test: cover iterator wrap path of MarchingSquares.cs

Remaining:
- 85 sequence points + 39 branch points in the polygon-merging pipeline, all
  unreachable due to the hardcoded Ps[x,0] row index in ProcessCell (line 276).
  Reaching them requires a production change (storing per-row polygons) or
  reflection into private methods, both forbidden.

Status:
BLOCKED (all remaining gaps are private methods on a statically unreachable
merge path caused by the Ps[x,0] hardcode).