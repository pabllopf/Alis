# Attempt 001

Target:
ImPlotP13.cs

Initial line coverage:
92.45% (147/159)

Initial branch coverage:
100%

Gaps:
- 12 PlotStairs overloads (short x4, int x4, uint x4) uncovered.

Root cause:
- Wrappers declared `ref short/int/uint xs, ref ys` but the call sites and the
  DllImport declarations passed the scalars BY VALUE. Native functions
  ImPlot_PlotStairs_S16PtrS16Ptr / _S32PtrS32Ptr / _U32PtrU32Ptr expect
  pointers ("Ptr" in the name). Passing the scalar value caused the native
  code to dereference a garbage address and HANG the test host.

Tests created:
- PlotStairs_Short_And_Int_And_Uint_Overloads_Execute

Production fix (documented defect):
- ImPlotNative.cs: added `ref` to xs/ys in 3 DllImport declarations.
- ImPlotP13.cs: added `ref` to xs/ys in 12 call sites.

Test result:
PASS (full project: 7660 passed, 14 skipped, 0 failed)

Coverage result:
100% lines, 100% branches for ImPlotP13.cs

Commit:
b5cb17c28

Remaining:
none