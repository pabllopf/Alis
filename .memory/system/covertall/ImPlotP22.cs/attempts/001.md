# Attempt 001

Target:
ImPlotP22.cs

Initial line coverage:
97.58% (161/165)

Initial branch coverage:
100%

Gaps:
- 4 short PlotLine overloads (lines 705, 718, 732, 747) uncovered.

Root cause:
- Wrappers declared `ref short xs, ref short ys` but passed `xs, ys` BY VALUE
  into ImPlot_PlotLine_S16PtrS16Ptr; the DllImport also declared them by
  value. The native function expects pointers; passing the scalar value made
  the native code dereference a garbage address and HANG the test host.

Tests created:
- PlotLine_ShortRef_Overloads_Execute_Inside_Plot

Production fix (documented defect):
- ImPlotNative.cs: added `ref` to xs/ys in the PlotLine_S16PtrS16Ptr DllImport.
- ImPlotP22.cs: added `ref` to xs/ys in the 4 call sites.

Test result:
PASS (full project: 7660 passed, 14 skipped, 0 failed)

Coverage result:
100% lines, 100% branches for ImPlotP22.cs

Commit:
b5cb17c28

Remaining:
none