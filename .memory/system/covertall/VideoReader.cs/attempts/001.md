# Attempt 001

Target:
./1_Presentation/Extension/Media/FFmpeg/src/Video/VideoReader.cs

Timestamp:
2026-08-17T10:45:00Z

Agent:
covertall-agent-videoreader

Baseline commit:
ec50af9690dd8708a170fa9d322cd49050fa50d0

## Initial coverage (baseline, before any work)

- `VideoReader` class: 62/62 lines (100%), 24/24 branches (100%)
- `VideoReader/<>c` (FirstOrDefault lambda): 1/1 lines (100%)
- `VideoReader/<LoadMetadataAsync>d__19` (async state machine): 18/37 lines (48.65%), 3/14 branches (21.43%)
- FILE TOTALS: 81/100 lines = 81.00%, 27/38 branches = 71.05%

## Gaps identified (all inside the async state machine MoveNext)

Uncovered sequence points (19): lines 144-149, 151, 153, 155, 157, 158, 160, 161, 163-167, 169.
All belong to the `if (videoStream != null)` stream-metadata block, its catch, and the catch exit.

Uncovered branches (11):
- line 143 `if (videoStream != null)` path true (vc=0)
- line 151 `videoStream.BitRate == null` both paths
- line 153 `videoStream.BitsPerRawSample == null` both paths
- line 155 `videoStream.Duration == null` both paths, and `metadata.Format.Duration ?? "0"` both paths
- line 165 `if (!ignoreStreamErrors)` both paths

## Production code analyzed

- `VideoReader.LoadMetadataAsync(bool)` (lines 126-178) is the only method containing the
  uncovered code.
- `videoStream = metadata.Streams.FirstOrDefault(x => x.CodecType.ToLower().Trim() == "video")`
  (line 142) can only be non-null when `metadata.Streams` is non-empty.
- `metadata` is created exclusively by `JsonNativeAot.Deserialize<VideoMetadata>(metadataJson)`
  (line 138). There is no other assignment to `Metadata` inside the class.
- The `VideoMetadata` deserialization is source-generated
  (Alis.Core.Aspect.Data.Generator SerializableSourceGenerator, obj/Debug generated file
  `VideoMetadata_Serialization.g.cs`): `Streams = DeserializeArray<MediaStream>(...)`.
- The generated `DeserializeArray<T>` (also `Deserialize2DArray<T>`, `DeserializeList<T>`,
  `DeserializeDictionary<...>` in the same generated file) only converts primitive types
  (string, enum, int, double, bool, float, long, decimal). For `MediaStream` (complex type)
  NO conversion case matches, so every array element is silently dropped
  (source of the helper: 6_Ideation/Data/generator/HelperMethodsGenerator.cs, lines 242-268).
- Result: `metadata.Streams` is ALWAYS an empty array regardless of the JSON input.

## Empirical verification that the branch is unreachable

1. Real ffprobe output for a generated H.264 MP4 (has `"codec_type": "video"`, width, height,
   bit_rate, duration, etc.) deserialized with the compiled library -> `Streams` length 0.
2. Minimal JSON `{"streams":[{"codec_type":"video"}]}` -> `Streams` length 0.
3. `{"Streams":[...]}` (capitalized) -> `Streams` length 0.
4. Full-fidelity fake-ffprobe JSON (codec_type video, codec_name, width 16, height 16,
   pix_fmt yuv420p10le, bit_rate 7728, bits_per_raw_sample 10, duration, format.duration)
   fed through the real public API in a test
   (`LoadMetadataAsync_WithVideoStreamInJson_PopulatesStreamMetadata`): the test FAILS with
   `Expected: 16 / Actual: 0` for Width, proving `videoStream` stays null.
5. The inner catch (lines 163-167) is only entered when an exception escapes the inner try,
   whose only statement is the `FirstOrDefault` on an always-empty array (no exception) -
   unreachable as well.

The probe test was removed afterwards to keep the project green (it asserts current broken
behavior and cannot be committed).

## Tests created

None that add coverage: every reachable sequence point and branch of the target file is
already covered by the existing suite (VideoReaderTest, VideoReaderTests,
VideoReaderCoverageTest, VideoReaderLoadCoverageTests, VideoReaderMetadataStreamTests).
All existing VideoReader tests pass (1561/1561 in the project).

## Test result

PASS (1561/1561) before and after the probe experiment.

## Final coverage

- `VideoReader` class: 62/62 lines (100%), 24/24 branches (100%)
- `VideoReader/<>c`: 1/1 lines (100%)
- `VideoReader/<LoadMetadataAsync>d__19`: 18/37 lines (48.65%), 3/14 branches (21.43%)
- FILE TOTALS: 81/100 lines = 81.00%, 27/38 branches = 71.05%

## Why additional tests cannot solve this

Every uncovered line/branch requires `videoStream != null`, which requires a non-empty
`metadata.Streams`, which the source-generated AOT deserializer can never produce for complex
element types. There is no JSON input, no fake executable, and no public API combination that
reaches it.

## Why reflection/dynamic code would be required

The uncovered code is inside the compiler-generated async state machine
`<LoadMetadataAsync>d__19::MoveNext` and its execution depends on a local variable
(`videoStream`) computed inside the method. Reaching it would require either:
- injecting a non-empty `Streams` into the deserialized `VideoMetadata` via reflection or
  IL manipulation (forbidden: rules 9 and 21, AOT constraints), or
- modifying the repository-wide source generator in 6_Ideation/Data/generator so that
  `DeserializeArray<T>` supports complex types (shared infrastructure change with
  repo-wide behavioral impact on every serializable type - forbidden during single-file
  remediation by rules 27/32 and AGENTS.md "What NOT to do").

## Commit

none (no coverage tests added; reachable code already fully covered)

## Remaining uncovered lines

144, 145, 146, 147, 148, 149, 151, 153, 155, 157, 158, 160, 161, 163, 164, 165, 166, 167, 169

## Remaining uncovered branches

line 143 (true), line 151 (both), line 153 (both), line 155 (four), line 165 (both)

## Status

BLOCKED - existing architectural limitation (source-generated AOT deserializer drops complex
array elements); reaching 100% would require a repo-wide generator change or reflection into
the async state machine, both prohibited.
