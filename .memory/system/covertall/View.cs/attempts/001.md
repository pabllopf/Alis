# Attempt 001

Target:
View.cs (1_Presentation/Extension/Graphic/Sfml/src/Render/View.cs)

Agent:
covertall-sfml-view

Timestamp:
2026-08-17T10:12Z — 2026-08-17T10:30Z

Initial coverage:
line 97.91% (47/48), branch 100% (2/2), methods 19/19

Gaps:
- Line 155 `sfView_reset(CPointer, rectangle);` inside `Reset(FloatRect)` — never executed (existing test only inspects parameter types via reflection).

Production code analyzed:
- View.cs (all constructors, properties, Reset/Move/Rotate/Zoom/ToString/Destroy, myExternal flag).
- ObjectBase.cs (base class: CPointer, Dispose, finalizer, Destroy abstraction).
- Csfml.cs (native library names).
- Native libraries resolution: src/runtimes/osx-x64 and osx-arm64, homebrew csfml 3.0.0.

Tests created:
- Reset_ShouldRebuildViewFromRectangle
- Reset_WhenViewWasMovedAndRotated_ShouldRestoreRectangleState

Test result:
FAIL — both throw System.EntryPointNotFoundException:
"Unable to find an entry point named 'sfView_reset' in shared library 'csfml-graphics'".
Failure occurs inside the P/Invoke call itself (View.sfView_reset), i.e. the native symbol is missing; it is not an assertion failure.

Root cause verification:
- nm on every CSFML library loadable by this arm64 machine:
  - src/runtimes/osx-arm64/native/libcsfml-graphics.dylib → CSFML 3.0, has sfView_getScissor, NO sfView_reset.
  - test bin copies (libcsfml-graphics.dylib, "libcsfml-graphics 2.dylib") → CSFML 3.0, NO sfView_reset.
  - /opt/homebrew/lib/libcsfml-graphics.dylib (Cellar csfml/3.0.0) → CSFML 3.0, NO sfView_reset.
- The ONLY library with sfView_reset is src/runtimes/osx-x64/native/csfml-graphics.dylib, which is a non-fat x86_64 binary (verified with lipo/file). An arm64 process cannot load it.
- CSFML 3.0 removed the sfView_reset API entirely (its view API drops reset; the symbol does not exist under any other name — verified with nm full symbol listing).
- No CSFML 2.x arm64 build exists anywhere in the repo, NuGet cache, or machine (checked ~/.nuget and all runtimes folders).

Why additional tests cannot help:
- Any invocation of View.Reset() throws EntryPointNotFoundException before any managed code after the call executes. The sequence point is inside the P/Invoke boundary and can only be hit if the native entry point exists.

Why reflection/dynamic code/AOT violation would be required:
- Not applicable — the blocker is a missing native symbol, not test access. Reflection cannot create a native entry point. Only installing/running against a CSFML 2.x arm64 library would help, which:
  1) requires modifying the machine environment outside the repository,
  2) is not available for arm64 anywhere on this machine,
  3) would change what the mandatory coverage command measures.

Final coverage:
line 97.91% (47/48), branch 100% (2/2), methods 19/19

Commit:
None — attempted tests were removed so the suite stays green on this machine; ViewTest.cs is identical to baseline.

Remaining:
- View.cs line 155 (sfView_reset call in Reset) — unexecutable on arm64 + CSFML 3.0. Coverable only on a platform with CSFML 2.x (e.g. x64 CI with the bundled osx-x64/linux CSFML 2.x runtimes).
