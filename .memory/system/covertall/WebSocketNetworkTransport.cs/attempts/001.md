# Attempt 001

Target:
WebSocketNetworkTransport.cs

Initial line coverage:
97.0% (164/169)

Initial branch coverage:
90.4% (47/52)

Gaps:
- AcceptConnectionsAsync fire-and-forget handler line (L297) and loop-exit
  conditions (L292).
- HandleClientAsync catch block (L325-328).
- ReceiveFromClientAsync loop-exit condition and socket disposal branch.
- StopAsync listener null-conditional branch.

Analysis:
- The accept loop exit requires the loop to re-evaluate its condition after
  AcceptTcpClientAsync returns. Deterministic trigger: cancel the StartAsync
  token, then connect a client - the accept completes, the loop re-checks and
  exits via the cancelled token (L292 off=194 path 1 + L297 covered).
- The HandleClientAsync catch fires when ReadHttpHeaderFromStreamAsync throws.
  A client sending an oversized header (20KB without \r\n\r\n) triggers the
  HttpHelper EntityTooLargeException deterministically.
- The receive-loop cancellation branch (L340 off=470 path 1) requires the
  loop to reach its top condition with a cancelled token. The socket's
  ReceiveAsync observes the linked token, so a mid-read cancellation throws
  and exits via the catch - the clean re-check is a sub-microsecond race.
- L365 off=545 path 0 (socket == null skip of socket?.Dispose): socket is
  always non-null in ReceiveFromClientAsync (assigned from
  AcceptWebSocketAsync), so the null branch is statically dead.
- L230 off=172 path 0 (_tcpListener == null skip of _tcpListener?.Stop):
  StopAsync early-returns unless the state is Connecting or Connected, and a
  null listener with those states only occurs inside StartAsync's synchronous
  Connecting window (a cross-thread race).

Tests created:
- AcceptLoop_WhenTokenCancelled_ExitsCleanly
- HandleClientAsync_WhenHandshakeFails_RemovesClient
- ReceiveLoop_WhenTokenCancelled_ExitsAndDisposesSocket
- StopAsync_AfterStartWithConnectedClient_StopsListener

Test result:
PASS (all Network tests pass)

Final line coverage:
100% (169/169)

Final branch coverage:
94.2% (49/52)

Commit:
test: cover socket lifecycle paths of WebSocketNetworkTransport.cs

Remaining:
- 3 branch points that are statically dead (socket never null; listener null
  only via the Connecting-window race) or a sub-microsecond race (token
  cancellation between frame-read completion and loop re-check). Reaching
  them deterministically requires production restructuring, forbidden.

Status:
BLOCKED (100% line coverage; the 3 remaining branches are dead or
race-dependent).