# Attempt 001 — EmscriptenWeb.cs

Timestamp: 2026-08-17
Agent: cover-agent-001
Target: 4_Operation/Graphic/src/Platforms/Web/EmscriptenWeb.cs

## Initial coverage

- Line: 82.23% (273/332)
- Branch: 9.09% (2/22)

## Gaps

All uncovered code is the success path of the P/Invoke wrappers:

- 13 try-block closing braces (454, 476, 496, 517, 625, 643, 661, 679, 697,
  952, 1097, 1115, 1133) — only reached when the native call completes normally.
- GetConnectedGamepads success logic (535-549), GetGamepadAxes (565-579),
  GetGamepadButtons (595-609), OpenFileDialog (880-885),
  PasteFromClipboard (931-936), GetLanguage (985-991) — only reached when
  `sf...`-style emscripten native calls return a valid pointer.

## Analysis

`[DllImport("emscripten", ...)]` cannot resolve on macOS. Every native call
throws DllNotFoundException inside the try, jumping to the catch/fallback path.
The success paths require a live WebAssembly/emscripten runtime (JS glue)
providing getConnectedGamepads, getGamepadAxes, openFileDialog, etc. No such
runtime exists on this platform, and fabricating a fake native "emscripten"
module would be environment manipulation / coverage gaming.

The existing test suite already covers every reachable catch/fallback path with
meaningful assertions (empty arrays, default scalars, null, "en", false).

## Tests created

None. The reachable surface is already fully covered; additional tests cannot
execute WASM-only code paths on macOS.

## Test result

PASS — 1559 passed, 613 skipped (WebOnly).

## Coverage result

- Line: 82.23%
- Branch: 9.09%

## Improvement

None possible without a WASM runtime.

## Commit

None.

## Remaining

WASM-only success paths. Status: BLOCKED.