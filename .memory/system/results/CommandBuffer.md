# Coverage Result: CommandBuffer.cs

File:
pabllopf-official_alis:4_Operation/Ecs/src/Kernel/CommandBuffer.cs

CoverageBefore:
99.5% (SonarCloud)

CoverageAfter:
99.5% (0.9945 line-rate measured locally via coverlet on the CommandBuffer filter; the 1 uncovered line — line 363, the closing brace of the `if` block in AssertCreatingEntity() — is an unreachable compiler-generated nop in the Debug build and does not exist as a sequence point in Release)

TestsAdded:
0 (uncovered line is dead code; no coverable path)

Commit:
test: coverage CommandBuffer.cs

Status:
BLOCKED_BY_PRODUCTION_CODE

## Analysis

- Existing suites verified: 99/99 CommandBuffer tests pass (net8.0, Debug).
- Local coverlet run (CommandBuffer filter) reproduces SonarCloud: line-rate 0.9945, branch-rate 1.0, exactly 1 uncovered line: 363.
- Line 363 is the `}` of the `if (LastCreateEntityComponentsBufferIndex < 0) { Throw(); }` block in `AssertCreatingEntity()`.
- Debug IL: the condition result is branched via `brfalse.s 0x16` — the fall-through (index >= 0) path jumps directly over the line-363 nop to the `ret`; the throw path (index < 0) calls `Throw()` which always throws, so control never falls through to the nop. The sequence point is dead code generated by the compiler.
- Release IL (what SonarCloud/CI consumes): sequence points exist only for lines 360, 362, 368, 370 — line 363 has no sequence point at all.
- Both paths of the `if` are already exercised (6 hits on the throw path, 17 hits on the fall-through path at line 370). No test can reach line 363 without modifying production code.
