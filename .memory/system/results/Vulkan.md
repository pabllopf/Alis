# Vulkan.cs

## File
1_Presentation/Extension/Graphic/Glfw/src/Vulkan.cs

## Coverage (SonarCloud, master)
- Line: 0.0%, Uncovered Lines: 16 (SonarCloud master)

## Local verification
- 8 `VulkanTests` pass (IsSupported, GetInstanceProcAddress variants,
  GetRequiredInstanceExtensions). The remaining members are
  `[ExcludeFromCodeCoverage]` DllImports.

## Analysis
Safe managed surface already exercised by existing tests; SonarCloud 0% on
master is a CI-environment artifact.

## Outcome
No new tests required.
