## PATTERN: RS2007 — Analyzer release header format

`AnalyzerReleases.Unshipped.md` requires `## Unshipped` (level 2 heading) rather than `### Unshipped` (level 3).

Fix: Change heading level from `###` to `##`.
